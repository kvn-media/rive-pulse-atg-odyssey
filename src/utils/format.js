// src/utils/format.js
const format = {
    capitalize: (str) => {
      return str.charAt(0).toUpperCase() + str.slice(1);
    },
  
    // Add more text formatting methods as needed
  };
  
  export default format;